メッセージキュー:メッセージを格納する. FIFO無制限
エラーキュー：FIFO無制限
inputモジュール辞書
outputモジュール辞書

メソッド
・make_module_dict:モジュール辞書作成
・push(queue):setmsg inputが呼ぶ
・pop(queue):getmsg outputが呼ぶ
・logger
・_run
・start
・exit

処理
make_modul_edict
for input in inputs: input.start()
while True: try: queue.get() except: 
event.wait()